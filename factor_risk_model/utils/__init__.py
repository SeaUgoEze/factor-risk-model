"""Shared helpers for the app layer - built in Step 2.

    helpers.py   date alignment, resampling, number formatting, and the
                 fetch-with-retry + on-disk cache utilities the app's data
                 layer relies on to stay fast (and polite to APIs).
"""
